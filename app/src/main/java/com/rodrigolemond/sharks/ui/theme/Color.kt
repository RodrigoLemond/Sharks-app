package com.rodrigolemond.sharks.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Definição das cores do tema Sharks App
 * Baseado na identidade visual do aplicativo com cores que representam
 * confiança (azul) e energia (laranja)
 */

// Cores Primárias - Azul Tubarão
val Primary = Color(0xFF0D47A1)
val PrimaryVariant = Color(0xFF002171)
val PrimaryLight = Color(0xFF5472D3)

// Cores Secundárias - Laranja Energia
val Secondary = Color(0xFFFF6F00)
val SecondaryVariant = Color(0xFFC43E00)
val SecondaryLight = Color(0xFFFFA040)

// Cores de Background
val Background = Color(0xFFFFFFFF)
val BackgroundDark = Color(0xFF121212)
val Surface = Color(0xFFFFFFFF)
val SurfaceDark = Color(0xFF1E1E1E)

// Cores de Texto
val TextPrimary = Color(0xFF000000)
val TextSecondary = Color(0xFF666666)
val TextOnPrimary = Color(0xFFFFFFFF)

// Cores de Status
val Success = Color(0xFF4CAF50)
val Error = Color(0xFFF44336)
val Warning = Color(0xFFFFC107)
val Info = Color(0xFF2196F3)
